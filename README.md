# Lounge Telegram Bot

Бот настроен под стартовый экран как на вашем примере:
- отправка фото при `/start`
- Reply-кнопки:
`🪪 Карта гостя`, `🧉 Меню`, `🛋 Бронь`, `🚕 Найти нас`

## Быстрый запуск

1. Установить зависимости:
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

2. Положить исходную картинку-логотип в:
`assets/lounge_source.jpg`

3. Проверить `.env`:
- `BOT_TOKEN`
- `GUEST_CARD_URL`
- `MENU_URL`
- `BOOKING_URL`
- `LOCATION_URL`

4. Запустить бота:
```bash
python3 bot.py
```
