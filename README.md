# Lounge Telegram Bot

Версия: 0.2

Бот настроен под стартовый экран как на вашем примере:
- отправка фото при `/start`
- inline-кнопки главного меню: `🪪 LEVEL`, `🧉 Меню`, `🛋 Бронь`, `🚕 Найти нас`

Дополнительно:
- LEVEL-карта с уровнями/скидками и базой карт (случайный 4-значный номер)
- учёт визитов (засчитываются админом по номеру карты, не чаще 1 раза в день)
- админ/супер-админ меню со статистикой и рейтингами

## Быстрый запуск

1. Установить зависимости:
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

2. Положить исходную картинку-логотип в:
`assets/lounge_source.jpg`

3. Проверить `.env`:
- `BOT_TOKEN`
- `WELCOME_IMAGE_PATH` (если нужно)
- `BOOKING_ADMIN`, `BOOKING_TEXT` (для брони)
- `LOCATION_2GIS_URL`, `NEWS_URL`, `PROHVAT72_URL` (для раздела "Найти нас")

4. Запустить бота:
```bash
python3 bot.py
```
